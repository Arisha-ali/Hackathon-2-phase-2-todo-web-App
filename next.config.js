/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: {}, // Enable server actions if needed for future functionality

  },
};

module.exports = nextConfig;
